import type { ReactNode } from "react";
import { AppNavigation } from "./AppNavigation";
import type { NavItem } from "./types";
type DashboardShellProps = { children: ReactNode; navItems: NavItem[]; greeting?: string; userName?: string; onCreate?: () => void; onSignOut?: () => void };
export function DashboardShell({ children, navItems, greeting = "Buenos días", userName = "", onCreate, onSignOut }: DashboardShellProps) { return <div className="flex min-h-screen bg-black"><AppNavigation items={navItems} onSignOut={onSignOut} profile={{ name: userName }} /><main className="min-w-0 flex-1"><div className="mx-auto w-full max-w-6xl px-6 py-10 sm:px-10 lg:px-16 lg:py-14"><header className="mb-20 sm:mb-24"><div className="flex flex-wrap items-start justify-between gap-6"><div><p className="text-base text-[#b8b8b8]">{greeting}, {userName}</p><h1 className="display-type mt-5 max-w-3xl text-6xl leading-[1.05] text-white sm:text-8xl">Un paso cada día<span className="text-[#ffb829]">.</span></h1></div><button type="button" onClick={onCreate} className="iris-button min-h-12 px-5 text-sm font-semibold uppercase tracking-[.08em]">+ Nueva tarea</button></div><p className="mt-8 max-w-xl text-lg leading-8 text-[#d0d0d0]">Pequeñas acciones, repetidas con calma, construyen la vida que quieres.</p></header>{children}</div></main></div>; }
