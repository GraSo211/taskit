import { IconArrow, IconPlus } from "./icons";
import { TaskCard } from "./TaskCard";
import type { Task } from "./types";
type RoutinePanelProps = { title: string; eyebrow?: string; tasks: Task[]; actionLabel?: string; onTaskToggle?: (id: string) => void; onEdit?: (id: string) => void; onAdd?: () => void };
export function RoutinePanel({ title, eyebrow = "Rutina de hoy", tasks, actionLabel = "Ver rutina", onTaskToggle, onEdit, onAdd }: RoutinePanelProps) { return <section aria-labelledby="routine-title"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-[#ffb829]">{eyebrow}</p><h2 id="routine-title" className="display-type mt-5 text-4xl text-white">{title}</h2></div><button type="button" onClick={onAdd} className="ghost-link" aria-label="Añadir tarea"><IconPlus className="size-5" /></button></div><div className="mt-8 divide-y divide-[#222]">{tasks.map((task) => <TaskCard key={task.id} task={task} onToggle={onTaskToggle} onEdit={() => onEdit?.(task.id)} />)}</div><button type="button" className="ghost-link mt-8 inline-flex items-center gap-2 text-sm underline underline-offset-4">{actionLabel}<IconArrow className="size-4" /></button></section>; }
