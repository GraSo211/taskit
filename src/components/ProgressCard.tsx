type ProgressCardProps = { label: string; value: number; detail?: string; tone?: "sage" | "coral" };
export function ProgressCard({ label, value, detail, tone }: ProgressCardProps) { const safe = Math.max(0, Math.min(100, value)); return <article className="py-2"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[.16em] text-[#b8b8b8]">{label}</p><p className="display-type mt-4 text-5xl text-white">{safe}<span className="text-2xl">%</span></p></div><span className={`text-base ${tone === "coral" ? "text-[#ffb829]" : "text-[#a88dff]"}`}>{detail}</span></div><div className="mt-7 h-2 rounded-full bg-[#2b2b31]"><div className={`h-2 rounded-full ${tone === "coral" ? "bg-[#ffb829]" : "bg-[#8052ff]"}`} style={{ width: `${safe}%` }} /></div></article>; }
